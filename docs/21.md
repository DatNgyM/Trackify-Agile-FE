# 21 nhánh feature – nói rõ từng nhánh làm gì

Mọi nhánh đều tách từ `develop`, làm xong thì merge về `develop`.

---

## FE – UI & Layout (5 nhánh)

| Nhánh | Làm gì |
|-------|--------|
| `feature/fe-ui-buttons-inputs` | Component Button, Input, Label trong `components/ui` |
| `feature/fe-ui-cards-modals` | Component Card, Modal, Dropdown trong `components/ui` |
| `feature/fe-layout-header-sidebar` | Header, Sidebar, Nav trong `components/layout` |
| `feature/fe-design-tokens-tailwind` | Màu, spacing, theme trong Tailwind (globals, config) |
| `feature/fe-framer-motion-animations` | Animation dùng chung (page transition, list, modal) với Framer Motion |

---

## Auth (3 nhánh)

| Nhánh | Làm gì |
|-------|--------|
| `feature/auth-nextauth-config` | Cấu hình NextAuth (lib/auth, app/api/auth/[...nextauth]) |
| `feature/auth-login-register-pages` | Trang login, register và form |
| `feature/auth-session-protected-routes` | Middleware/guard bảo vệ dashboard, redirect khi chưa đăng nhập |

---

## Projects (3 nhánh)

| Nhánh | Làm gì |
|-------|--------|
| `feature/projects-list-create` | Server Actions + trang list projects + form tạo project |
| `feature/projects-edit-delete-settings` | Sửa, xóa project + trang settings |
| `feature/projects-board-view` | Trang board (danh sách project dạng board/card) |

---

## Tasks (4 nhánh)

| Nhánh | Làm gì |
|-------|--------|
| `feature/tasks-list-create` | Server Actions + trang list tasks + form tạo task |
| `feature/tasks-edit-detail` | Sửa task + trang chi tiết task |
| `feature/tasks-board-kanban` | Board Kanban (kéo thả theo status) |
| `feature/tasks-filter-by-project` | Lọc task theo project, tìm kiếm |

---

## Dashboard & Profile (2 nhánh)

| Nhánh | Làm gì |
|-------|--------|
| `feature/dashboard-layout-nav` | Layout dashboard, sidebar, menu điều hướng |
| `feature/dashboard-profile-page` | Trang profile (xem/sửa thông tin user) |

---

## CI/CD & Docs (3 nhánh)

| Nhánh | Làm gì |
|-------|--------|
| `feature/cicd-page` | Trang dashboard/cicd (nội dung, link, trạng thái) |
| `feature/docker-compose-scripts` | Docker Compose, script npm (docker:up, db:*) |
| `feature/docs-readme-setup` | README, STRUCTURE, hướng dẫn clone/run cho cả nhóm |

---

## DB & Shared (3 nhánh)

| Nhánh | Làm gì |
|-------|--------|
| `feature/db-prisma-schema-migrations` | Prisma schema (User, Project, Task), migrations |
| `feature/db-seed-types` | Seed data (nếu cần) + thư mục types/ |
| `feature/shared-submodule-validations` | Git submodule `modules/shared`, Zod schemas dùng chung |

---

**Tổng: 21 nhánh.** Khi GV bảo gom bớt thì xem [branch.md](./branch.md) (bảng 7 nhánh gộp).
