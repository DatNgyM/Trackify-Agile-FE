# Unit tests (Vitest)

Chạy từ root project:

```bash
npm run test        # một lần
npm run test:watch  # watch mode
```

File test nằm dưới `test/`, cấu trúc gần giống `lib/`, `validations/` để dễ tìm.
